import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as s,c as n,e as t}from"./app-VLgNDF8W.js";const a={},o=t(`<h1 id="css-操作" tabindex="-1"><a class="header-anchor" href="#css-操作" aria-hidden="true">#</a> CSS 操作</h1><h2 id="操作元素节点的-style-属性" tabindex="-1"><a class="header-anchor" href="#操作元素节点的-style-属性" aria-hidden="true">#</a> 操作元素节点的 style 属性</h2><p>操作 HTML 元素的 <code>style</code> 属性：</p><ul><li><p>使用元素节点的相关方法操作 <code>style</code> 属性</p><ul><li><p><code>element.getAttribute()</code></p><p><strong>语法</strong> ： <code>element.getAttribute(attributeName)</code></p><p><strong>描述</strong> ： 返回当前元素的 <code>attributeName</code> 属性值。</p></li><li><p><code>element.setAttribute()</code></p><p><strong>语法</strong> ： <code>element.setAttribute(attributeName, attributeValue)</code></p><p><strong>描述</strong> ： 设置当前元素的 <code>attributeName</code> 属性值为 <code>attributeValue</code>。如果已存在，则更新属性值。</p></li><li><p><code>element.removeAttribute()</code></p><p><strong>语法</strong> ： <code>element.removeAttribute(attributeName)</code></p><p><strong>描述</strong> ： 移除当前元素的 <code>attributeName</code> 属性。</p></li></ul><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// &lt;div id=&quot;divId&quot; style=&quot;color: red; font-size: 20px&quot;&gt;div content&lt;/div&gt;</span>

<span class="token keyword">let</span> divDOM <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">getElementById</span><span class="token punctuation">(</span><span class="token string">&#39;divId&#39;</span><span class="token punctuation">)</span>
divDOM<span class="token punctuation">.</span><span class="token function">getAttribute</span><span class="token punctuation">(</span><span class="token string">&#39;style&#39;</span><span class="token punctuation">)</span> <span class="token comment">// color: red; font-size: 20px</span>
divDOM<span class="token punctuation">.</span><span class="token function">setAttribute</span><span class="token punctuation">(</span><span class="token string">&#39;style&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;color: blue; font-size: 30px&#39;</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><code>style</code> 属性不仅可以使用字符串读写，其本身也是一个对象（<code>CSSStyleDeclaration</code> 接口），可以直接读写个别属性</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// &lt;div id=&quot;divId&quot; style=&quot;color: red; font-size: 20px&quot;&gt;div content&lt;/div&gt;</span>

<span class="token keyword">let</span> divDOM <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">getElementById</span><span class="token punctuation">(</span><span class="token string">&#39;divId&#39;</span><span class="token punctuation">)</span>
divDOM<span class="token punctuation">.</span>style<span class="token punctuation">.</span>color <span class="token comment">// red</span>
divDOM<span class="token punctuation">.</span>style<span class="token punctuation">.</span>fontSize <span class="token comment">// 20px</span>
divDOM<span class="token punctuation">.</span>style<span class="token punctuation">.</span>color <span class="token operator">=</span> <span class="token string">&#39;blue&#39;</span>
divDOM<span class="token punctuation">.</span>style<span class="token punctuation">.</span>fontSize <span class="token operator">=</span> <span class="token string">&#39;30px&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><h2 id="cssstyledeclaration-接口" tabindex="-1"><a class="header-anchor" href="#cssstyledeclaration-接口" aria-hidden="true">#</a> CSSStyleDeclaration 接口</h2><p><code>CSSStyleDeclaration</code> 接口表示一个 CSS 声明块，它是一个 CSS 属性及其值的有序映射表，提供样式信息和各种与样式相关的属性方法和属性。以下 API 返回该接口实例：</p><ul><li><code>HTMLElement.style</code> ，用于操作单个元素节点 <code>style</code> 属性。该实例<strong>只返回行内样式</strong>，并不包含外部样式表和嵌入样式表。</li><li><code>CSSStyleSheet</code> 实例，用于操作单个 CSS 规则的 <code>style</code> 属性。例如 <code>document.styleSheets[0].cssRules[0].style</code> 返回文档中第一个样式表的第一条 CSS 规则。</li><li><code>window.getComputedStyle()</code> 返回值。</li></ul><p><code>style</code> 属性的值是一个 <code>CSSStyleDeclaration</code> 实例，对象中的属性与 CSS 规则一一对应。</p><ul><li>CSS 属性中的连字符 <code>-</code> 需转换为驼峰命名法，例如 <code>background-color</code> 转换为 <code>backgroundColor</code>。</li><li>如果 CSS 属性名是 JavaScript 中的保留字，则需在 CSS 属性名前添加 <code>&#39;css&#39;</code> 。例如 <code>float</code> 转换为 <code>style[&#39;float&#39;]</code>。</li><li>对象属性值为字符串，设置时需添加单位，例如 <code>style.width = &#39;100px&#39;</code>。</li></ul><h3 id="cssstyledeclaration-实例属性" tabindex="-1"><a class="header-anchor" href="#cssstyledeclaration-实例属性" aria-hidden="true">#</a> CSSStyleDeclaration 实例属性</h3><ul><li><code>CSSStyleDeclaration.cssFloat</code> ：返回 <code>float</code> 属性值。如果属性名为 JavaScript 中的保留字，则需使用 <code>cssFloat</code> 代替 <code>float</code> 。</li><li><code>CSSStyleDeclaration.cssText</code> ：获取或设置 CSS 声明块的文本内容。可以使用 <code>element.style.cssText = &#39;&#39;</code> 删除元素的所有行内样式。</li><li><code>CSSStyleDeclaration.length</code> ：返回当前 CSS 声明块中声明的 CSS 属性数量。</li><li><code>CSSStyleDeclaration.parentRule</code> ：返回包含当前 CSS 声明块的 CSS 规则（<code>CSSRule</code> 实例）。</li></ul><h3 id="cssstyledeclaration-实例方法" tabindex="-1"><a class="header-anchor" href="#cssstyledeclaration-实例方法" aria-hidden="true">#</a> CSSStyleDeclaration 实例方法</h3><ul><li><p><code>element.style.getPropertyPriority()</code></p><p><strong>语法</strong> ： <code>element.style.getPropertyPriority(property)</code></p><p><strong>描述</strong> ： 返回 <code>property</code> 属性的优先级。如果存在则返回 <code>&quot;important&quot;</code> 否则返回空字符串。</p></li><li><p><code>element.style.getPropertyValue()</code></p><p><strong>语法</strong> ： <code>element.style.getPropertyValue(property)</code></p><p><strong>描述</strong> ： 返回 <code>property</code> 属性的值。</p></li><li><p><code>element.style.item()</code></p><p><strong>语法</strong> ： <code>element.style.item(index)</code></p><p><strong>描述</strong> ： 返回 <code>index</code> 位置的 CSS 属性名。</p><ul><li>如果未提供 <code>index</code> ，则会报错。</li><li>如果 <code>index</code> 超出范围，则返回 <code>null</code> 。</li></ul></li><li><p><code>element.style.removeProperty()</code></p><p><strong>语法</strong> ： <code>element.style.removeProperty(property)</code></p><p><strong>描述</strong> ： 移除 <code>property</code> 属性，并返回移除的属性值。</p></li><li><p><code>element.style.setProperty()</code></p><p><strong>语法</strong> ： <code>element.style.setProperty(property, value, priority)</code></p><p><strong>描述</strong> ： 设置 <code>property</code> 属性的值为 <code>value</code> ，优先级为 <code>priority</code> 。<code>value</code> 和 <code>priority</code> 可选。</p></li></ul><h2 id="css-属性检测" tabindex="-1"><a class="header-anchor" href="#css-属性检测" aria-hidden="true">#</a> CSS 属性检测</h2><p>检测浏览器是否支持某个 CSS 属性。</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">isPropertySupported</span><span class="token punctuation">(</span><span class="token parameter">property</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span>property <span class="token keyword">in</span> document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>style<span class="token punctuation">)</span> <span class="token keyword">return</span> <span class="token boolean">true</span>
  <span class="token keyword">var</span> prefixes <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token string">&#39;Moz&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;Webkit&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;O&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;ms&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;Khtml&#39;</span><span class="token punctuation">]</span>
  <span class="token keyword">var</span> prefProperty <span class="token operator">=</span> property<span class="token punctuation">.</span><span class="token function">charAt</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toUpperCase</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> property<span class="token punctuation">.</span><span class="token function">substr</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>

  <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">var</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> prefixes<span class="token punctuation">.</span>length<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>prefixes<span class="token punctuation">[</span>i<span class="token punctuation">]</span> <span class="token operator">+</span> prefProperty <span class="token keyword">in</span> document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>style<span class="token punctuation">)</span> <span class="token keyword">return</span> <span class="token boolean">true</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">return</span> <span class="token boolean">false</span>
<span class="token punctuation">}</span>

<span class="token function">isPropertySupported</span><span class="token punctuation">(</span><span class="token string">&#39;background-clip&#39;</span><span class="token punctuation">)</span>
<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="css-接口" tabindex="-1"><a class="header-anchor" href="#css-接口" aria-hidden="true">#</a> CSS 接口</h2><p>CSS 接口是一个工具接口，无法创建该类型的对象，但可以使用它的属性和方法。</p><ul><li><p>CSS 接口属性</p></li><li><p>CSS 接口方法</p><ul><li><p><code>CSS.escape()</code></p><p><strong>语法</strong> ： <code>CSS.escape(string)</code></p><p><strong>描述</strong> ： 转义 CSS 选择器中的特殊字符。</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// &lt;div id=&quot;foo#bar&quot;&gt;</span>

<span class="token comment">// 元素 id 属性中包含了 # 字符，需要转义后，才能通过 document.querySelector 获取元素</span>
document<span class="token punctuation">.</span><span class="token function">querySelector</span><span class="token punctuation">(</span><span class="token string">&#39;#&#39;</span> <span class="token operator">+</span> <span class="token constant">CSS</span><span class="token punctuation">.</span><span class="token function">escape</span><span class="token punctuation">(</span><span class="token string">&#39;foo#bar&#39;</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
<span class="token comment">// 即： document.querySelector(&#39;#foo\\\\#bar&#39;)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><code>CSS.supports()</code></p><p><strong>语法</strong> ： <code>CSS.supports(property, value)</code></p><p><strong>描述</strong> ： 检测浏览器是否支持某个 CSS 属性。</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 第一种写法</span>
<span class="token constant">CSS</span><span class="token punctuation">.</span><span class="token function">supports</span><span class="token punctuation">(</span><span class="token string">&#39;transform-origin&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;5px&#39;</span><span class="token punctuation">)</span> <span class="token comment">// true</span>

<span class="token comment">// 第二种写法：不能带有分号，否则结果不准确</span>
<span class="token constant">CSS</span><span class="token punctuation">.</span><span class="token function">supports</span><span class="token punctuation">(</span><span class="token string">&#39;display: table-cell&#39;</span><span class="token punctuation">)</span> <span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></li></ul><h2 id="window-getcomputedstyle" tabindex="-1"><a class="header-anchor" href="#window-getcomputedstyle" aria-hidden="true">#</a> window.getComputedStyle()</h2><p><strong>语法</strong> ： <code>window.getComputedStyle(element, pseudoElement)</code></p><p><strong>描述</strong> ： 返回 <code>element</code> 元素的所有 CSS 属性（动态更新的 <code>CSSStyleDeclaration</code> 实例）。如果 <code>pseudoElement</code> 为 <code>null</code> ，则返回元素的所有 CSS 属性，否则返回伪元素的所有 CSS 属性。</p><ul><li><code>CSSStyleDeclaration</code> 实例返回的 CSS 值都是绝对单位。比如，长度是像素单位（返回值包括 <code>px</code> 后缀），颜色是 <code>rgb(#, #, #)</code>或 <code>rgba(#, #, #, #)</code> 格式。</li><li>CSS 规则的简写形式无效。比如，读取 <code>margin</code> 属性的值，不能直接读，只能读 <code>marginLeft</code> 、 <code>marginTop</code> 等属性； <code>font</code> 属性也是不能直接读的，只能读 <code>font-size</code> 等单个属性。</li><li>如果读取 CSS 原始的属性名，要用方括号运算符，比如：<code>styleObj[&#39;z-index&#39;]</code>；如果读取骆驼拼写法的 CSS 属性名，可以直接读取 <code>styleObj.zIndex</code>。</li><li>方法返回的 <code>CSSStyleDeclaration</code> 实例的 <code>cssText</code> 属性无效，返回 <code>undefined</code> 。</li></ul><p><strong>参数</strong> ：</p><ul><li><code>element</code> ：必选，要获取样式的元素节点。</li><li><code>pseudoElement</code> ：可选，伪元素（比如 <code>:before</code> 、 <code>:after</code> 、 <code>:first-line</code> 、 <code>:first-letter</code> 等）。</li></ul><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">let</span> div <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">querySelector</span><span class="token punctuation">(</span><span class="token string">&#39;div&#39;</span><span class="token punctuation">)</span>
<span class="token keyword">let</span> styleObj <span class="token operator">=</span> window<span class="token punctuation">.</span><span class="token function">getComputedStyle</span><span class="token punctuation">(</span>div<span class="token punctuation">)</span>
styleObj<span class="token punctuation">.</span>backgroundColor
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="stylesheet-、-cssstylesheet-接口" tabindex="-1"><a class="header-anchor" href="#stylesheet-、-cssstylesheet-接口" aria-hidden="true">#</a> StyleSheet 、 CSSStyleSheet 接口</h2><p><code>StyleSheet</code> 接口表示一个样式表，包括 <code>&lt;style&gt;</code> 元素内嵌样式表、<code>&lt;link&gt;</code> 元素外部样式表。</p><ul><li><code>document.styleSheets</code> 返回当前文档中所有 <code>StyleSheet</code> 实例（即所有样式表）。</li><li>如果是 <code>&lt;style&gt;</code> 元素内嵌样式表，可以通过获取该元素的 <code>sheet</code> 属性获取 <code>StyleSheet</code> 实例。</li></ul><p><code>CSSStyleSheet</code> 接口表示一个 CSS 样式表，允许检查和编辑样式表中的 CSS 规则。从父类型 <code>StyleSheet</code> 继承属性和方法。</p><h3 id="stylesheet-、-cssstylesheet-实例属性" tabindex="-1"><a class="header-anchor" href="#stylesheet-、-cssstylesheet-实例属性" aria-hidden="true">#</a> StyleSheet 、 CSSStyleSheet 实例属性</h3><ul><li><p><code>StyleSheet.disabled</code> ：获取或设置样式表是否禁用。手动设置 <code>disabled</code> 属性为 <code>true</code>，等同于在 <code>&lt;link&gt;</code> 元素中设置 <code>alternate stylesheet</code>，表示该样式表不会生效。</p></li><li><p><code>StyleSheet.href</code> ：只读属性。返回样式表的 URL 地址。对于 <code>&lt;style&gt;</code> 元素内嵌样式表返回为 <code>null</code> 。</p></li><li><p><code>StyleSheet.media</code> ：返回 <code>MediaList</code> 实例对象，表示样式表的媒体类型。</p></li><li><p><code>StyleSheet.title</code> ：返回样式表的 <code>title</code> 标题属性。</p></li><li><p><code>StyleSheet.type</code> ：返回样式表的 <code>type</code> 类型属性，通常为 <code>text/css</code>。</p></li><li><p><code>StyleSheet.ownerNode</code> ：返回拥有当前样式表的节点，通常是 <code>&lt;style&gt;</code> 或 <code>&lt;link&gt;</code>。对于由其他样式表导入的样式表，返回 <code>null</code> 。</p></li><li><p><code>StyleSheet.parentStyleSheet</code> ：CSS 的 <code>@import</code> 命令允许在样式表中加载其他样式表，该属性返回包含当前样式表的样式表（<code>StyleSheet</code> 实例）。如果当前样式表是顶层样式表，则返回 <code>null</code> 。</p></li><li><p><code>CSSStyleSheet.cssRules</code> ：返回样式表的所有 CSS 规则（<code>CSSRuleList</code> 实例）。每条规则可以使用其 <code>cssText</code> 属性获取对应 CSS 规则的字符串，使用其 <code>style</code> 属性可以读写 CSS 规则的样式。</p></li><li><p><code>CSSStyleSheet.ownerRule</code> ：返回通过 <code>@import</code> 命令引入当前样式表的 CSS 规则（<code>CSSRule</code> 实例）。如果当前样式表不是由 <code>@import</code> 命令引入的，则返回 <code>null</code> 。</p></li></ul><h3 id="stylesheet-、-cssstylesheet-实例方法" tabindex="-1"><a class="header-anchor" href="#stylesheet-、-cssstylesheet-实例方法" aria-hidden="true">#</a> StyleSheet 、 CSSStyleSheet 实例方法</h3><ul><li><p><code>CSSStyleSheet.deleteRule()</code></p><p><strong>语法</strong> ： <code>CSSStyleSheet.deleteRule(index)</code></p><p><strong>描述</strong> ： 删除样式表中指定位置的 CSS 规则。</p></li><li><p><code>CSSStyleSheet.insertRule()</code></p><p><strong>语法</strong> ： <code>CSSStyleSheet.insertRule(rule, index)</code></p><p><strong>描述</strong> ： 在样式表中指定位置 <code>index</code> （可选，默认值为 <code>0</code>）插入 CSS 规则 <code>rule</code> ，并返回新插入规则的位置索引。</p><ul><li>如果插入位置大于现有规则的数目，会报错。</li><li>浏览器对脚本在样式表里面插入规则有很多限制，最好放在 <code>try...catch</code> 里使用。</li></ul></li><li><p><code>CSSStyleSheet.replace()</code></p><p><strong>语法</strong> ： <code>CSSStyleSheet.replace(text)</code></p><p><strong>描述</strong> ： 以异步方式将样式表的内容替换为传入的内容 <code>text</code> 。返回一个与 <code>CSSStyleSheet</code> 对象解析的 <code>Promise</code> 。</p></li><li><p><code>CSSStyleSheet.replaceSync()</code></p><p><strong>语法</strong> ： <code>CSSStyleSheet.replaceSync(text)</code></p><p><strong>描述</strong> ： 以同步方式将样式表的内容替换为传入的内容 <code>text</code> 。</p></li></ul><h2 id="cssrulelist-接口" tabindex="-1"><a class="header-anchor" href="#cssrulelist-接口" aria-hidden="true">#</a> CSSRuleList 接口</h2><p>CSS 规则列表 <code>CSSRuleList</code> 是一个只读的类数组对象，包含了 CSS 规则（<code>CSSRule</code> 实例）。</p><ul><li>获取 <code>CSSRuleList</code> 实例，一般通过 <code>StyleSheet.cssRules</code> 属性获取。</li><li><code>CSSRuleList</code> 实例中，每一条 CSS 规则（<code>CSSRule</code> 实例）可以通过 <code>rules.item(index)</code> 或 <code>rules[index]</code> 获取。</li><li>CSS 规则的条数可以通过 <code>rules.length</code> 获取。</li><li>添加或删除规则不能在 <code>CSSRuleList</code> 实例操作，只能在父元素 <code>StyleSheet</code> 实例上操作，可以通过 <code>CSSStyleSheet.insertRule()</code> 和 <code>CSSStyleSheet.deleteRule()</code> 方法。</li></ul><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// &lt;style id=&quot;myStyle&quot;&gt;</span>
<span class="token comment">//   h1 { color: red; }</span>
<span class="token comment">//   p { color: blue; }</span>
<span class="token comment">// &lt;/style&gt;</span>

<span class="token keyword">let</span> styleSheet <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">getElementById</span><span class="token punctuation">(</span><span class="token string">&#39;myStyle&#39;</span><span class="token punctuation">)</span><span class="token punctuation">.</span>sheet
<span class="token keyword">var</span> cssRuleList <span class="token operator">=</span> styleSheet<span class="token punctuation">.</span>cssRules
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="cssrule-接口" tabindex="-1"><a class="header-anchor" href="#cssrule-接口" aria-hidden="true">#</a> CSSRule 接口</h2><p><code>CSSRule</code> 接口表示一条 CSS 规则。JavaScript 通过 <code>CSSRule</code> 接口操作 CSS 规则，一般通过 <code>CSSRuleList</code> 实例（<code>StyleSheet.cssRules</code>）获取 <code>CSSRule</code> 实例。</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// &lt;style id=&quot;myStyle&quot;&gt;</span>
<span class="token comment">//   .myClass {</span>
<span class="token comment">//     color: red;</span>
<span class="token comment">//     background-color: yellow;</span>
<span class="token comment">//   }</span>
<span class="token comment">// &lt;/style&gt;</span>

<span class="token keyword">let</span> styleSheet <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">getElementById</span><span class="token punctuation">(</span><span class="token string">&#39;myStyle&#39;</span><span class="token punctuation">)</span><span class="token punctuation">.</span>sheet
<span class="token keyword">let</span> cssRuleList <span class="token operator">=</span> styleSheet<span class="token punctuation">.</span>cssRules
<span class="token keyword">let</span> ruleItem <span class="token operator">=</span> cssRuleList<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>CSSRule</code> 实例属性：</p><ul><li><p><code>CSSRule.cssText</code> ：获取或设置 CSS 规则的文本内容。如果 CSS 规则是通过 <code>@import</code> 命令引入的，则返回 <code>@import &#39;url&#39;</code> 。</p></li><li><p><code>CSSRule.parentStyleSheet</code> ：返回包含当前 CSS 规则的样式表（<code>StyleSheet</code> 实例）。</p></li><li><p><code>CSSRule.parentRule</code> ：返回包含当前 CSS 规则父规则。如果不存在父规则（即当前规则为顶层规则），则返回 <code>null</code> 。</p></li></ul><h2 id="cssstylerule-接口" tabindex="-1"><a class="header-anchor" href="#cssstylerule-接口" aria-hidden="true">#</a> CSSStyleRule 接口</h2><p><code>CSSStyleRule</code> 接口表示一条 CSS 样式规则，继承自 <code>CSSRule</code> 接口。</p><p><code>CSSStyleRule</code> 实例属性：</p><ul><li><p><code>CSSStyleRule.selectorText</code> ：获取或设置当前规则的 CSS 选择器。</p></li><li><p><code>CSSStyleRule.style</code> ：返回 <code>CSSStyleDeclaration</code> 实例，表示 CSS 规则的样式声明（即选择器后大括号中的部分）。<code>CSSStyleDeclaration</code> 实例的 <code>cssText</code> 属性，返回样式声明的文本内容。</p></li></ul><h2 id="cssmediarule-接口" tabindex="-1"><a class="header-anchor" href="#cssmediarule-接口" aria-hidden="true">#</a> CSSMediaRule 接口</h2><p><code>CSSMediaRule</code> 接口表示单个 CSS <code>@media</code> 规则。</p><p><code>CSSMediaRule</code> 实例属性：</p><ul><li><code>CSSMediaRule.media</code> ：返回 <code>MediaList</code> 实例，表示 <code>@media</code> 规则的对象。</li><li><code>CSSMediaRule.conditionText</code> ：返回 <code>@media</code> 规则的生效条件。</li></ul><h2 id="window-matchmedia" tabindex="-1"><a class="header-anchor" href="#window-matchmedia" aria-hidden="true">#</a> window.matchMedia()</h2><p><strong>语法</strong> ： <code>window.matchMedia(mediaQueryString)</code></p><p><strong>描述</strong> ： 返回 <code>MediaQueryList</code> 实例，表示指定的媒体查询字符串 <code>mediaQueryString</code> 是否匹配当前文档。</p><ul><li><p><code>MediaQueryList</code> 实例属性</p><ul><li><code>MediaQueryList.matches</code> ：返回布尔值，表示指定的媒体查询字符串是否匹配当前文档。</li><li><code>MediaQueryList.media</code> ：返回媒体查询字符串。</li></ul></li><li><p><code>MediaQueryList</code> 实例方法</p><ul><li><code>MediaQueryList.onchange</code> ：监听函数，当媒体查询字符串匹配当前文档时，执行监听函数。</li><li><code>MediaQueryList.addListener()</code> ：添加监听函数。当媒体查询字符串匹配当前文档时，执行监听函数。</li><li><code>MediaQueryList.removeListener()</code> ：移除监听函数。</li></ul></li></ul>`,55),c=[o];function l(d,p){return s(),n("div",null,c)}const u=e(a,[["render",l],["__file","css.html.vue"]]);export{u as default};
