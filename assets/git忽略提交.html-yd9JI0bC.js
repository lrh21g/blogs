import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as i,c as o,e as c}from"./app-VLgNDF8W.js";const d={},a=c(`<h1 id="git-gitignore" tabindex="-1"><a class="header-anchor" href="#git-gitignore" aria-hidden="true">#</a> Git .gitignore</h1><h2 id="git-忽略规则" tabindex="-1"><a class="header-anchor" href="#git-忽略规则" aria-hidden="true">#</a> Git 忽略规则</h2><h3 id="git-忽略规则优先级" tabindex="-1"><a class="header-anchor" href="#git-忽略规则优先级" aria-hidden="true">#</a> Git 忽略规则优先级</h3><ul><li>从命令行中读取可用的忽略规则</li><li>当前目录定义的规则</li><li>父级目录定义的规则，依次地推</li><li><code>$GIT_DIR/info/exclude</code> 文件中定义的规则</li><li><code>core.excludesfile</code> 中定义的全局规则</li></ul><h3 id="git-忽略规则匹配语法" tabindex="-1"><a class="header-anchor" href="#git-忽略规则匹配语法" aria-hidden="true">#</a> Git 忽略规则匹配语法</h3><ul><li><code>空格</code> : 不匹配任意文件，可作为分隔符，可用反斜杠转义</li><li><code># 开头</code> : 标识注释，可以使用反斜杠进行转义</li><li><code>! 开头</code> : 标识否定，该文件将会再次被包含，如果排除了该文件的父级目录，则使用 <code>!</code> 也不会再次被包含。可以使用反斜杠进行转义</li><li><code>/ 结束</code> : 只匹配文件夹以及在该文件夹路径下的内容，但是不匹配该文件</li><li><code>/ 开始</code> : 的模式匹配项目跟目录</li><li>如果一个模式不包含斜杠，则它匹配相对于当前 <code>.gitignore</code> 文件路径的内容，如果该模式不在 <code>.gitignore</code> 文件中，则相对于项目根目录</li><li><code>**</code> : 匹配多级目录，可在开始，中间，结束 ?通用匹配单个字符</li><li><code>[]</code> : 通用匹配单个字符列表</li></ul><p>示例：</p><ul><li><code>bin/</code> : 忽略当前路径下的 <code>bin</code> 文件夹，该文件夹下的所有内容都会被忽略，不忽略 <code>bin</code> 文件</li><li><code>/bin</code> : 忽略根目录下的bin文件</li><li><code>/*.c</code> : 忽略 <code>cat.c</code> ，不忽略 <code>build/cat.c</code></li><li><code>debug/*.obj</code> : 忽略 <code>debug/io.obj</code> ，不忽略 <code>debug/common/io.obj</code> 和 <code>tools/debug/io.obj</code></li><li><code>**/foo</code> : 忽略 <code>/foo</code>, <code>a/foo</code>, <code>a/b/foo</code> 等</li><li><code>a/**/b</code> : 忽略 <code>a/b</code>, <code>a/x/b</code>, <code>a/x/y/b</code> 等</li><li><code>!/bin/run.sh</code> : 不忽略 <code>bin</code> 目录下的 run.sh 文件</li><li><code>*.log</code> : 忽略所有 <code>.log</code> 文件</li><li><code>config.php</code> : 忽略当前路径的 <code>config.php</code> 文件</li></ul><h2 id="gitignore规则不生效" tabindex="-1"><a class="header-anchor" href="#gitignore规则不生效" aria-hidden="true">#</a> .gitignore规则不生效</h2><p><code>.gitignore</code> 只能忽略那些原来没有被 <code>track</code> 的文件，如果某些文件已经被纳入了版本管理中，则修改 <code>.gitignore</code> 是无效的。</p><p>解决方法：删除本地缓存（改变成未 <code>track</code> 状态），再提交。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 删除本地缓存（改变成未 \`track\` 状态）</span>
$ <span class="token function">git</span> <span class="token function">rm</span> <span class="token parameter variable">-r</span> <span class="token parameter variable">--cached</span> <span class="token builtin class-name">.</span>

<span class="token comment"># 提交</span>
$ <span class="token function">git</span> <span class="token function">add</span> <span class="token builtin class-name">.</span>
$ <span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&#39;update .gitignore&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,12),n=[a];function l(s,t){return i(),o("div",null,n)}const p=e(d,[["render",l],["__file","git忽略提交.html.vue"]]);export{p as default};
